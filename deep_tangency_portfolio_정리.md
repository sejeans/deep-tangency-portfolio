# Deep Tangency Portfolio 논문 정리 (한글, 비전공자용)

> 논문: **Deep Tangency Portfolio**
> 저자: Guanhao Feng, Liang Jiang, Junye Li, Yizhi Song, Yuanzhi Wang
> 초판 2021년 11월 / 본 버전 2026년 6월 (SSRN 3971274)
> 키워드: 접점 포트폴리오(Tangency Portfolio), 딥러닝, Deep Factor, 회사채(Corporate Bond)

---

## 0. 30초 요약

- **하고 싶은 것**: "샤프지수(Sharpe Ratio)가 가장 높은 포트폴리오"를 딥러닝으로 직접 만든다.
- **기존 방식의 문제**: 교과서 방식은 (기대수익률 μ)과 (공분산 행렬 Σ)을 추정한 뒤 `w = Σ⁻¹μ` 로 비중을 계산하는데, 이 두 입력값의 추정 오차가 너무 커서 실전에서 성능이 망가진다.
- **이 논문의 해법**: μ와 Σ를 **아예 추정하지 않는다.** 신경망이 **포트폴리오 비중 자체를 바로 출력**하게 하고, 손실함수를 "샤프지수 최대화"로 둔다.
- **핵심 구조**: 이미 잘 알려진 벤치마크(예: 채권 시장지수)를 **그대로 깔아두고**, 거기에 132개 특성(characteristics)에서 뽑아낸 **딥 팩터(deep factor, 롱숏 포트폴리오) 하나를 얹는다.** (= "성능 향상 장치, enhancement device")
- **결과**: 딥 팩터 단독 샤프지수 1.0~1.49, 벤치마크 + 딥 팩터 = **딥 접점 포트폴리오 샤프지수 2.13~2.21** (아웃오브샘플 기준). 기존 FF5(0.88), IPCA5(1.48), RP-PCA5(0.93)를 크게 앞선다.

---

## 1. 배경 지식 (금융 모르는 사람을 위한 최소 개념)

복제하려면 아래 5개 개념만 알면 됩니다.

### 1-1. 초과수익률 (excess return)
자산 수익률에서 무위험이자율(1개월 미국 국채 금리)을 뺀 값.
`r = 자산수익률 − 무위험이자율`. 이 논문의 모든 수익률은 초과수익률입니다.

### 1-2. 샤프지수 (Sharpe Ratio, SR)
`SR = 평균 수익률 / 수익률의 표준편차`
"위험 1단위당 얼마를 벌었나"를 재는 지표. 월별 데이터면 연율화를 위해 `× √12`를 곱합니다.
**이 논문의 목적함수 = 이 값을 최대로 만드는 것.**

### 1-3. 접점 포트폴리오 (Tangency Portfolio) = 평균-분산 효율(MVE) 포트폴리오
모든 가능한 포트폴리오 중에서 **샤프지수가 최대인 단 하나의 포트폴리오**.
이론적 해: `w = Σ⁻¹ μ`, 그때의 최대 제곱 샤프지수는 `SR² = μ' Σ⁻¹ μ`.
문제는 자산이 3,000개면 Σ가 3000×3000이라 추정이 사실상 불가능하다는 것.

### 1-4. 팩터 (factor)와 롱숏 포트폴리오
"팩터"란 결국 **어떤 규칙으로 만든 하나의 포트폴리오의 수익률 시계열**입니다.
- 시장 팩터(MKT): 모든 채권을 (동일가중 또는 시가총액가중) 사서 보유.
- 롱숏 팩터: 어떤 특성 점수가 높은 자산은 **매수(long, 비중 +)**, 낮은 자산은 **공매도(short, 비중 −)**. 비중 합이 0이라 "무비용(zero-cost)" 포트폴리오.

### 1-5. 특성 (characteristic)
각 자산이 매달 갖는 숫자 속성. 예: 채권 듀레이션, 신용등급, 지난달 수익률, 주식 시가총액, 옵션 내재변동성 등.
이 논문은 자산 하나당 **132개** 특성을 씁니다.

---

## 2. 목적 (Objective)

### 2-1. 논문이 풀려는 문제
1. **고차원 문제**: 자산 수 N이 수천 개면 `Σ⁻¹μ`는 추정 오차가 폭발한다.
2. **희소성(sparsity) 가정의 실패**: 기존 팩터모형(예: Fama-French 3/5팩터)은 소수의 특성만 선형으로 쓴다. 그런데 실제 수익률 신호는 **"dense"** (많은 특성에 널리 퍼져 있음). 그래서 소수 팩터로는 효율적 프론티어를 **span(포괄)하지 못한다.**
3. **비선형성 무시**: 특성 간 상호작용·비선형 관계를 선형모형은 못 잡는다.
4. **2단계 추정의 오차 누적**: "수익률 예측 → 그 예측으로 포트폴리오 구성"은 예측 오차가 포트폴리오 단계에서 증폭된다.

### 2-2. 논문의 목표
> 벤치마크 전략(시장 포트폴리오 등)을 **경제학적 사전지식(prior)** 으로 고정해 두고,
> 132개 고차원 특성에서 신경망으로 **딥 팩터 1개**를 뽑아 얹어서,
> **샤프지수를 직접 최대화**하는 접점 포트폴리오를 만든다.

딥 팩터가 하는 두 가지 역할:
- **(i) 헤지 역할**: 샤프지수 최대화를 목적으로 하면, 최적화가 자동으로 벤치마크와 **음의 상관관계**를 갖는 포트폴리오를 찾아낸다 → 시장이 폭락할 때 딥 팩터가 오른다.
- **(ii) 프론티어 확장**: 벤치마크가 놓친 다중시장(채권+주식+옵션) 신호를 포착.

### 2-3. 왜 하필 회사채(corporate bond) 시장인가?
- 주식보다 가격 형성이 불투명하고, 표준 팩터(term, default, CAPM)가 횡단면 수익률 차이를 잘 설명하지 못한다.
- 즉 "개선의 여지"가 가장 큰 시장이라서 방법론의 효과가 극명하게 드러난다.
- (프레임워크 자체는 주식 등 다른 자산군에도 그대로 적용 가능하다고 논문이 명시)

---

## 3. 사용한 데이터와 데이터 형식

### 3-1. 데이터 출처 (전부 유료 DB)

| 구분 | 출처 | 용도 |
|---|---|---|
| 회사채 체결가 | **TRACE (Enhanced)** | 장내/장외 채권 거래 내역(일중 체결 단위) |
| 채권 메타정보 | **Mergent FISD** | 발행일, 발행액, 만기, 쿠폰 종류/이율, 채권 유형, 신용등급, 이자지급 주기, 발행사 정보 |
| 무위험이자율 | **CRSP** | 1개월 국채(T-bill) 금리 |
| 주식 특성 | CRSP / Compustat | 61개 주식 특성 |
| 옵션 특성 | OptionMetrics 계열 | 30개 옵션 특성 |
| 비교용 팩터 | Ken French 데이터 라이브러리 | 주식 SMB, HML |

> **복제 시 주의**: TRACE Enhanced, FISD, OptionMetrics는 모두 **WRDS 유료 구독**이 필요합니다. 개인이 그대로 재현하기는 어렵고, 4장의 "대체 데이터 전략"을 참고하세요.

### 3-2. 데이터 클리닝 절차 (그대로 따라할 순서)

Dick-Nielsen (2009, 2014) 표준 절차 + 추가 필터:

1. TRACE 원자료에서 **중복·오류 거래 제거** (취소/정정 거래 처리).
2. 다음 채권 **제외**:
   - (i) 미국 공모시장에 상장/거래되지 않는 채권
   - (ii) 구조화채권, MBS, ABS, 정부기관채, 미국 국채, 주식연계채권
   - (iii) 전환사채(convertible) — 옵션 성격이 수익률 계산을 왜곡
   - (iv) 잔존만기 **2년 미만**
   - (v) 가격이 **$5 미만 또는 $1,000 초과**
   - (vi) 일중 거래대금 **$10,000 미만**인 체결 기록
   - (vii) 결제일이 **2일 초과**인 거래
3. **일별 가격** = 그날 일중 체결가의 **거래량 가중평균** (Bessembinder et al. 2008).
4. **월별 수익률** 계산 (쿠폰·경과이자 포함한 총수익):

   ```
   ĩ r_{i,t} = (P_{i,t} + AI_{i,t} + C_{i,t}) / (P_{i,t-1} + AI_{i,t-1}) − 1
   ```
   - `P` = 월말 가격, `AI` = 경과이자(accrued interest), `C` = 쿠폰 지급액
   - **"월말"의 정의**: 해당 월의 **마지막 5거래일** (여러 건이면 가장 마지막 관측치 사용)
   - **"월초"의 정의**: 해당 월의 **처음 5거래일**
   - 우선순위: `(t−1월말 → t월말)` 수익률을 우선 사용 → 보유기간 중복 방지
5. **초과수익률**: `r_{i,t} = ĩ r_{i,t} − rf_t` (rf = CRSP 1개월 T-bill)
6. **유동성 필터**: 매월 **시가총액(잔액 × 월말가격) 상위 3,000개 채권**만 남김.
   - 이유: 상위 3,000개는 하위 대비 호가스프레드 50% 좁고, 무거래일 70% 적음.
   - 3,000개가 안 되는 달이 10개 있음(그래도 2,800개 이상) → 부족분은 수익률·특성을 **0으로 대체**.

### 3-3. 최종 데이터 형식 (★ 복제 시 만들어야 할 표)

**패널 데이터(panel data)** 형태입니다.

| 항목 | 값 |
|---|---|
| 기간 | **2004년 7월 ~ 2020년 12월** (총 198개월) |
| 고유 채권 수 | 14,796개 |
| 채권-월 관측치 | **590,991개** |
| 매월 자산 수 (N) | 3,000 (고정, 균형패널로 맞춤) |
| 특성 수 (K) | **132개** |

원시 데이터는 `bond_id × month` 를 키로 하는 롱 포맷:

```
bond_id, month, excess_return, char_001, char_002, ..., char_132
```

학습 시에는 **월별 3차원 텐서**로 재구성합니다:

```
X : (T개월, N=3000, K=132)   ← 시점 t-1의 특성
y : (T개월, N=3000)          ← 시점 t의 초과수익률
Rb: (T개월, P+1)             ← 벤치마크 팩터 수익률 (시장 팩터 등)
```

> 데이터가 균형(balanced)이어야 텐서 연산이 편하므로, 결측 채권은 수익률 0 / 특성 0으로 채워 3,000개를 유지합니다.

### 3-4. 특성 132개의 구성

#### (1) 채권 특성 41개
- **기본**: AGE(발행 후 경과연수), RATING(신용등급), T2M(잔존만기), SIZE(발행잔액), DUR(듀레이션), YTM(만기수익률)
- **유동성**: LIQ_BPW, LIQ_ROLL(Roll 척도), LIQ_P_HL(고저 스프레드), LIQ_P_FHT, LIQ_AMIHUD, LIQ_STD_AMIHUD, LIQ_TC_IQR, LIQ_RANGE, LIQ_RANGE_M, LIQ_TRADE(체결 건수), TURN_CB(회전율), BARQ(1개월 일평균 거래대금), STD_BARQ_1MOM
- **수익률 분포**: STR(단기 반전, t−1), MOM6(t−2~t−6 모멘텀), MOM12(t−7~t−12), LTR(장기반전 t−13~t−48), VARIANCE, SKEW, KURT, COSKEW, ISKEW, VAR5/VAR10(VaR 5%/10%, 3년), MKT_RVAR, TERM_DEF_RVAR
- **공분산(베타)**: MKT_BETA, DEF_BETA, TERM_BETA, LIQ_BETA, DRF_BETA, CRF_BETA, LRF_BETA, VIX_BETA, UNC_BETA(거시 불확실성 베타)

#### (2) 주식 특성 61개
모멘텀 / 밸류 / 투자 / 수익성 / 규모 / 무형자산 6개 카테고리.
예: MOM1M, MOM6M, MOM12M, MOM36M, MOM60M, BM, EP, CFP, SP, DY, ME, ME_IA, AGR, ACC, OP, GMA, ROA, ROE, LEV, CASH, TURN, DOLVOL, ILL, BASPREAD, BETA, MAXRET, ZEROTRADE, SUE, ABR, HIRE, NOA, ...

*(주식-채권 연결 근거: Merton 구조모형상 채권과 주식은 같은 기업가치에 대한 청구권 → 두 시장은 통합되어 있음. Schaefer & Strebulaev 2008, Kelly et al. 2023)*

#### (3) 옵션 특성 30개 (이 논문의 차별점)
- **내재변동성(IV) 계열**: IVSLOPE, IVVOL, IVRV, IVRV_RATIO, ATM_CIVPIV, SKEWIV, IVD, DCIV, DPIV, ATM_DCIVPIV, IVARUD30
- **거래량 계열**: NOPT, SO, DSO, VOL, PCRATIO, PBA, TOI
- **위험중립 적률**: MFVU, MFVD, RNS1M/3M/6M/9M/12M(위험중립 왜도), RNK1M/3M/6M/9M/12M(위험중립 첨도)

### 3-5. 특성 전처리 (★ 매우 중요 — 반드시 그대로)

매달, **횡단면(cross-section)** 으로:
1. 각 특성을 **순위(rank)** 로 변환
2. **[−1, +1] 구간**으로 스케일링, 횡단면 평균이 0이 되도록
3. **결측값은 0으로 대체**

```python
# 월별 횡단면 rank → [-1, 1]
def cs_rank_standardize(x):          # x: (N,) 한 달치 한 특성
    r = pd.Series(x).rank(pct=True)  # 0~1
    z = 2 * r - 1                    # -1~1
    return z.fillna(0.0).values
```

이유: 순위 변환은 **이상치·데이터 오류의 영향을 크게 줄여줍니다** (Kelly et al. 2019, Kozak et al. 2020).

### 3-6. 학습 시 수익률 처리
- **인샘플 수익률만 윈저라이즈(winsorize)**: 매월 횡단면 5% / 95% 분위수로 절단.
- **아웃오브샘플은 절대 윈저라이즈 안 함** (실제 극단값 포함해서 평가).
- 주식·옵션 특성은 **PERMNO** 로 채권 데이터에 병합. 한 기업이 여러 주식을 발행했으면 **가장 먼저 발행된 주식**만 사용.

---

## 4. 사용한 방법론 (핵심)

### 4-1. 출발점: 전통적 방식

최적 포트폴리오 수익률과 비중:
```
R_t^opt = w_{t-1}' r_t ,        w_{t-1} = Σ_{t-1}^{-1} μ_{t-1}
최대 제곱 샤프지수: SR² = μ' Σ^{-1} μ
```

Brandt–Santa-Clara–Valkanov(2009)의 **파라메트릭 포트폴리오** 방식은 비중을 이렇게 씁니다:
```
w_{t-1} = w̃_{t-1} + z_{t-1} κ
```
- `w̃` = 시가총액 가중치(= 시장 포트폴리오)
- `z` = 표준화된 특성 (J개, J는 작음)
- `κ` = 학습할 계수 벡터 (J×1)

그러면 최적 포트폴리오는 `R^opt = R_m + κ'f` 즉 **시장 팩터 + 특성 기반 팩터들의 조합**이 됩니다.

**한계 2가지**: (i) 특성 몇 개만 쓴다, (ii) 선형이다.

### 4-2. 이 논문의 확장: 딥 파라메트릭 포트폴리오

특성 개수를 K개(=132, K ≫ J)로 늘리고, 선형 함수를 **신경망**으로 바꿉니다.

```
w_{i,t-1} = w̃_{i,t-1} + θ · w_d(z_{i,t-1}; Φ)          ...(6)
```
- `w̃` : 벤치마크(시장) 비중
- `w_d(·)` : 신경망이 만드는 **롱숏 비중** (횡단면 합 = 0)
- `Φ` : 신경망 파라미터, `θ` : 딥 팩터의 배분 비중(스칼라)

따라서 포트폴리오 수익률은:
```
R_t^opt = R_{m,t} + θ · R_{d,t}          ...(7)
```
= **시장 팩터 + θ × 딥 팩터**. 딱 2개 자산의 조합 문제로 축소됩니다!

벤치마크 팩터를 여러 개(P개) 넣고 싶으면:
```
R_t^opt = R_{m,t} + θ_p' R_{p,t} + θ_d · R_{d,t}      ...(9)
```

### 4-3. 신경망 구조: 132개 특성 → "딥 특성" 1개

**핵심 아이디어**: 132개 특성을 압축해서 **자산당 스칼라 값 1개(deep characteristic)** 로 만든다. 그 값으로 전통적인 "정렬(sorting)" 방식처럼 롱숏 포트폴리오를 만든다.

```
Z⁽⁰⁾_{i,t-1} = [z_1, z_2, ..., z_132]'          (입력: 132차원)
Z⁽ˡ⁾_{i,t-1} = G( A⁽ˡ⁾ Z⁽ˡ⁻¹⁾_{i,t-1} + b⁽ˡ⁾ )   (l = 1, ..., L)
Z⁽ᴸ⁾_{i,t-1} ∈ ℝ                                 (출력: 스칼라 1개)
```
- `G(·)` = **tanh** 활성화함수 (element-wise)
- 자산 간 상호작용 없음 — **각 자산에 동일한 네트워크를 독립적으로 적용** (즉 자산 축은 배치 차원처럼 취급)
- 학습 파라미터: `{A⁽ˡ⁾ ∈ ℝ^{K_l × K_{l-1}}, b⁽ˡ⁾ ∈ ℝ^{K_l}}`

결과: 매달 `1 × N` 크기의 **딥 특성 벡터** `Z⁽ᴸ⁾_{t-1}`.

### 4-4. 딥 특성 → 롱숏 비중 (h 함수)

두 가지 방식을 비교합니다.

#### (A) 선형 랭킹 (linear ranking)
```
W_{t-1} = (a / N) · Z⁽ᴸ⁾_{t-1}
```
단순하지만 성능이 나쁨.

#### (B) 소프트맥스 랭킹 (softmax ranking) ← **논문의 최종 채택안**

전통적 포트폴리오 정렬(상위 30% 매수 / 하위 30% 공매도)은 계단함수라 **미분 불가능** → 경사하강법을 못 씀.
그래서 이걸 **미분 가능한 부드러운 버전**으로 근사합니다.

`x = Z⁽ᴸ⁾_{t-1}` 이라 할 때:
```
h(x) = softmax(x⁺) − softmax(x⁻)          ...(16)

여기서   x⁺_i = −a₁ · exp(−a₂ · x_i)      ← 롱 레그
        x⁻_i = −a₁ · exp(+a₂ · x_i)      ← 숏 레그
        softmax(x_i) = exp(x_i) / Σ_j exp(x_j)
```
- 첫 항 = **롱 포지션 비중**, 둘째 항 = **숏 포지션 비중** (대칭)
- 하이퍼파라미터: **a₁ = 50, a₂ = 8** (튜닝 결과)
  → 이 값에서 자산의 **약 50~70%가 중간 순위로 밀려 비중이 거의 0**이 됨 = 전통적 정렬과 유사한 모습
- **정규화**: 롱 레그 비중 합 = **+1**, 숏 레그 비중 합 = **−1**
- 장점: 롱·숏 양쪽에서 **극단적 포지션이 생기지 않음**

딥 팩터 수익률:
```
R_{d,t} = W_{t-1} · r_t          ...(18)
```

> **논문 결론**: 소프트맥스 랭킹 > 선형 랭킹. 즉 **"비선형 롱숏 구조를 명시적으로 강제하는 것"이 성능의 핵심**입니다.

### 4-5. 손실함수 (★ 여기가 이 논문의 심장)

이론적 목적: 평균 조건부 제곱 샤프지수 최대화
```
max_{θ,Φ}  (1/T) Σ_t SR²_{t-1}(R_t^opt)          ...(10)
```

파라미터가 시간불변이고, 특성이 조건부 기대수익률·공분산을 충분히 담고 있다고 가정하면 **비조건부(unconditional) 제곱 샤프지수 최대화**로 단순화됩니다:

```
F̃_t = [R_{b,t}' , R_{d,t}]'          (벤치마크 팩터들 + 딥 팩터)

SR²(F̃_t) = E(F̃_t)' · Cov(F̃_t)^{-1} · E(F̃_t)          ...(19)
```

> **여기가 포인트**: `Cov(F̃)`는 `(P+1)×(P+1)` 크기 — 팩터가 2개면 **2×2 역행렬**만 구하면 됩니다.
> 3000×3000 공분산 역행렬 대신 2×2 역행렬. 이것이 이 방법이 고차원 문제를 우회하는 방식입니다.

실제 최소화하는 손실함수:
```
L = E_{B~D_B, D~Dropout(p)} [ exp{ −SR²( R_t^opt(B, D) ) } ]          ...(20)
```
- `B` = 미니배치 (훈련 데이터에서 랜덤 샘플링)
- `D` = 확률 p의 드롭아웃 마스크
- `exp(−SR²)` 형태 → SR²이 커질수록 손실이 0에 수렴 (수치적으로 안정적, 항상 양수)
- 정규화는 **L1/L2 대신 드롭아웃** 사용 (신경망에서 일반적으로 더 좋다 — Chen, Pelger, Zhu 2024)

> 논문 강조: 이건 **수익률 예측 모형이 아니라 "롱숏 팩터 자동 생성 모형"** 입니다.
> 132차원을 1차원으로 눌러서 정렬한 뒤 상하위 약 30%의 스프레드만 취하므로 과적합되기 어렵습니다.

### 4-6. 학습 세팅과 하이퍼파라미터 (★ 그대로 복제 가능)

**표본 분할**
| 구간 | 기간 | 용도 |
|---|---|---|
| 인샘플 | 2004-07 ~ 2014-06 (120개월) | 학습 + 검증 |
| 아웃오브샘플 | 2014-07 ~ 2020-12 (78개월) | 최종 평가 |

**2-fold 결정론적 교차검증** (Figure A1)
```
1st fold: [2004.07~2009.06 = 학습] [2009.07~2014.06 = 검증] | [2014.07~2020.12 = 홀드아웃]
2nd fold: [2004.07~2009.06 = 검증] [2009.07~2014.06 = 학습] | [2014.07~2020.12 = 홀드아웃]
```
→ 두 fold의 검증 손실을 **평균** → 최적 하이퍼파라미터 선택 → **인샘플 전체(2004.07~2014.06)로 재학습**.
→ OOS 결과는 **전부 인샘플 추정 파라미터를 고정**한 채 계산 (룩어헤드 편향 없음).

**하이퍼파라미터 (Table A4) — 총 120개 조합 탐색**

| 기호 | 의미 | 후보 | **선택값** |
|---|---|---|---|
| a₁ | 식(16) 파라미터 | 50 | **50** |
| a₂ | 식(16) 파라미터 | 5, 8, 10 | **8** |
| HDN | 은닉층 노드 수 | 66 | **66** |
| BTCH | 배치 크기 (개월 단위) | 10, 30 | **10** |
| LR | 학습률 | 1e-3, 5e-3, 1e-2, 5e-2, 1e-1 | **1e-3** |
| DR | 드롭아웃 비율 | 0.05, 0.1, 0.2, 0.3 | **0.1** |
| EPCH | 에폭 수 | 400 | **400** |
| OPT | 옵티마이저 | Adam | **Adam** |
| L | 은닉층 수 | 1, 2, 3 | **2** ← 대부분의 경우 최적 |

**구현 환경**: PyTorch 1.10.2, Intel Xeon Gold 6230 96코어 / RAM 314GB.

> 참고: 배치 크기가 "10개월"이라는 점에 주의. 샘플 단위가 **개별 자산이 아니라 "월(month)"** 입니다.
> 샤프지수를 계산하려면 시계열이 필요하므로, 배치 하나 = 10개월치 (10, 3000, 132) 텐서입니다.

### 4-7. 비교 대상 (Alternative Portfolios)

| 모형 | 구성 |
|---|---|
| **벤치마크 MKT** | 회사채 시장 팩터 — **동일가중(EW)** 및 **시가총액가중(VW)** 둘 다 사용 |
| **FF5** | 채권 3팩터(시장 VW, TERM, DEF) + 주식 2팩터(SMB, HML) |
| **IPCA5** | Kelly-Pruitt-Su(2019) 방식 잠재팩터 5개, 동일 TRACE 데이터 + 132개 특성 |
| **RP-PCA5** | Lettau-Pelger(2020) 방식 잠재팩터 5개, 상위 20개 중요 특성으로 만든 **60개 3분위 포트폴리오** 기반 |

- TERM 팩터 = 장기 국채 수익률 − 1개월 T-bill
- DEF 팩터 = 장기 회사채 수익률 − 장기 국채 수익률
- SMB, HML = Ken French 라이브러리

**ML 벤치마크** (4.5절): Lasso, Ridge, PCA(K=5), 표준 신경망 NN1/NN2/NN3.
- 목적함수 2종: 샤프지수 최대화(Max SR) vs 예측 MSE 최소화(Min MSE)
- 포트폴리오 구성 2종: 선형 가중 / 횡단면 정렬(상하위 10%, 20%, 30% 롱숏)

### 4-8. 변수 중요도 측정법

딥 특성에 대한 **평균 절대 기울기(average absolute gradient)** — Chen, Pelger, Zhu(2024) 방식:
```
Sensitivity(z_j) = (1/C) · Σ_i Σ_t | ∂Z⁽ᴸ⁾_{i,t-1} / ∂z_j |
```
j = 1, ..., 132. C는 스케일 정규화 상수. PyTorch에서 `autograd.grad`로 계산 가능.

### 4-9. 거래비용 반영 (Online Appendix IV)

Kelly, Palhares, Pruitt(2023) 방식 — **회전율 기반**.

1. 비중을 **지수평활(exponential smoothing)** 로 "느리게" 만듦:
   ```
   w̃_d(z_{t-1}) = (1−γ) · w_d(z_{t-1}) + γ · w̃_d(z_{t-2}),    초기값 w̃_d(z_{-1}) = w_d(z_0)
   ```
2. 순수익률:
   ```
   R̃_{d,t} = w̃_d(z_{t-1}) · r_t − λ · | w̃_d(z_{t-1}) − w̃_d(z_{t-2}) |
   ```
   - `λ` = 편도 거래비용, **15bp / 20bp** 두 수준
   - 회전율 = 연속된 평활 비중 벡터 간 **ℓ1 거리**
3. `γ` ∈ {0.0, 0.1, 0.3, 0.5, 0.7, 0.9}
4. 거래비용 포함 시 극단적 공매도를 막기 위해 **접점 포트폴리오 비중을 [−2, +3]으로 제한**

---

## 5. 결과 (Results)

### 5-1. 딥 팩터 자체의 성능 (OOS, Table 2)

연율화 샤프지수:

| 신경망 층 수 | 선형랭킹 (EW) | 선형랭킹 (VW) | **소프트맥스 (EW)** | **소프트맥스 (VW)** |
|---|---|---|---|---|
| 1층 | 0.38 | 0.76 | 0.55 | 1.21 |
| **2층** | 0.34 | 1.12 | **1.00** | **1.49** |
| 3층 | 0.55 | 0.95 | 0.71 | 0.09 |

**2층 소프트맥스 딥 팩터 (EW 벤치마크)**: 월평균 0.16% (t = 2.65), 최소 −1.54%, 최대 +2.37%, **최대낙폭 2.48%**
**2층 소프트맥스 딥 팩터 (VW 벤치마크)**: 월평균 0.19% (t = 4.54), 최소 −1.12%, 최대 +1.52%, **최대낙폭 1.71%**

→ 결론: **2층이 최적**, **소프트맥스 > 선형**.

### 5-2. 딥 접점 포트폴리오 vs 대안 (Table 3)

**Panel A — 딥 접점 포트폴리오** (소프트맥스 랭킹, 대괄호는 최대낙폭 %)

| 벤치마크 | MKT 단독 | L1 | **L2** | L3 |
|---|---|---|---|---|
| 동일가중(EW) | 0.85 [0.89] | 0.80 | **2.13\*\*\*** [3.07] | 0.95\*\*\* |
| 시가총액가중(VW) | 0.90 [0.88] | 1.43\*\*\* | **2.21\*\*\*** [1.09] | 1.08\*\*\* |

**Panel B — 대안 포트폴리오**

| | FF5 | IPCA5 | RP-PCA5 |
|---|---|---|---|
| 단독 SR | 0.88 | 1.48 | 0.93 |
| 최대낙폭 | 2.98% | — | 4.35% |
| **+ 딥 팩터 추가 시** | **1.89\*\*\*** [0.93] | **1.91\*\*\*** [1.09] | **2.04\*\*\*** [1.04] |

→ **딥 팩터를 어디에 붙여도 샤프지수가 크게 올라가고 최대낙폭은 줄어든다.**
→ 샤프지수 개선의 유의성은 Barillas–Shanken(2017) 검정으로 확인 (\*\*\* = 1% 유의).

> IPCA 관련 주석: Kelly et al.(2023)은 IPCA5로 SR 6.23을 보고했으나, 이 논문에서는 1.48. 이유 두 가지 —
> (1) 표본이 훨씬 큼 (590,991 vs 144,933 채권-월), (2) 이 논문은 파라미터를 인샘플에 고정(비교 가능성 확보)한 반면 원논문은 확장윈도우 사용. (확장윈도우로 다시 해봐도 1.67 수준)

### 5-3. 왜 잘 되는가? — 음의 상관관계 (Figure 2, 3)

- 인샘플·아웃오브샘플 모두에서 **시장 팩터와 딥 팩터는 강한 음의 상관관계**.
- 산점도의 **좌하단 사분면(둘 다 하락)에 점이 거의 없음** → 동시 손실이 드묾.
- **COVID-19 국면(2020년 초)**: 시장 팩터는 급락, **딥 팩터는 높은 양(+) 수익률**.
- 누적수익률: 딥 팩터와 딥 접점 포트폴리오는 코로나 기간에도 계속 상승, 시장 팩터만 큰 손실.

**중요**: 이 헤지 성질은 사람이 넣은 제약이 아니라, **샤프지수 최대화 목적함수에서 자동으로(endogenously) 나온 것**입니다. 분산투자 원리상 최적화가 벤치마크와 음의 상관을 갖는 방향을 찾는 게 유리하기 때문입니다.

### 5-4. 팩터 스패닝 회귀 (Table 4)

`R_t = α + β' f_t + ε_t` 형태. (2층, 소프트맥스, EW 벤치마크)

| 설명 팩터 | 대상 | α (%) | t-stat | Adj. R² |
|---|---|---|---|---|
| FF5 | 딥 팩터 | 0.26 | 5.99 | 44.3% |
| FF5 | 딥 접점 포트폴리오 | 0.21 | 5.99 | 6.68% |
| IPCA5 | 딥 팩터 | 0.21 | 4.20 | 48.0% |
| IPCA5 | 딥 접점 포트폴리오 | 0.16 | 3.89 | 16.3% |
| RP-PCA5 | 딥 팩터 | 0.27 | 6.38 | 44.0% |
| RP-PCA5 | 딥 접점 포트폴리오 | 0.22 | 6.43 | 7.83% |

→ **모든 알파가 통계적으로 매우 유의**. 기존 관측가능/잠재 팩터로는 딥 팩터를 설명하지 못한다.
→ 딥 팩터/딥 접점 포트폴리오는 SMB에 음(−), HML에 양(+)의 로딩. TERM, DEF 로딩은 모두 음이지만 유의하지 않음.

### 5-5. 어떤 특성이 중요한가 (Figure 4)

**상위 5개 (전부 채권 특성)**: STR(단기반전) > DUR(듀레이션) > T2M(잔존만기) > TERM_BETA > MOM12
그다음: TURN_CB, **MOM1M(주식 단기반전, 7위)**, MKT_BETA, MOM6, SKEW, DEF_BETA, barQ, **IVVOL(ATM 내재변동성의 변동성, 13위)**

두 가지 발견:
1. **딥 특성은 "dense"** — 소수 변수가 아니라 광범위한 변수들이 함께 기여. → **희소성(sparsity) 가정에 대한 반증** (Kozak et al. 2020, Kozak & Nagel 2024와 일치)
2. **채권 특성이 지배적**이지만, 주식·옵션 특성도 상위권에 등장 → 시장 간 정보 스필오버가 실재.

경제적 해석: 단기반전과 듀레이션 계열이 상위 → **유동성 프리미엄과 만기 위험 보상**을 체계적으로 포착하는 것이 성과의 원천.

### 5-6. 특성 종류별 기여도 (Table 5) — ★ 가장 인상적인 결과

동일가중 벤치마크, 소프트맥스 랭킹 기준 OOS 샤프지수:

| 층 수 | MKT | **채권+주식+옵션 (132개)** | 채권+주식 (102개) | 채권만 (41개) | 주식만 (61개) |
|---|---|---|---|---|---|
| L1 | 0.85 | 0.80 | 1.04\*\*\* | 0.76 | 0.83 |
| **L2** | 0.85 | **2.13\*\*\*** | 1.01\*\*\* | 0.70 | 0.57 |
| L3 | 0.85 | 0.95\*\* | 0.83 | 0.24 | 0.59 |

→ **옵션 변수를 빼면 2.13 → 1.01로 반토막.**
→ 채권만(0.70), 주식만(0.57)은 시장 팩터(0.85)보다도 나쁨.
→ **결론: 세 시장의 정보를 모두 합쳐야만 효율적 프론티어를 span 할 수 있다.**

### 5-7. 구조와 비선형성이 정말 중요한가? (Table 6)

**Panel A — 표준 ML 포트폴리오 (OOS SR)**

| 모형 | Max SR | Min MSE | 상위/하위 10% | 20% | 30% |
|---|---|---|---|---|---|
| NN1 | 1.23 | 0.85 | **1.52** | 1.44 | 1.40 |
| NN2 | 1.08 | 0.83 | 1.06 | 1.14 | 1.19 |
| NN3 | 1.28 | 0.84 | 0.93 | 0.95 | 0.96 |
| LASSO | 0.84 | 1.06 | 0.86 | 0.81 | 0.85 |
| Ridge | **1.54** | 1.32 | 1.01 | 1.12 | 1.10 |
| PCA | 0.72 | 0.72 | 0.56 | 0.61 | 0.64 |

→ 최고가 1.54 (Ridge). **딥 접점 포트폴리오 2.13에 한참 못 미침.**
→ 즉 "그냥 딥러닝을 쓴 것"이 아니라 **벤치마크를 prior로 두는 경제적 구조**가 성능의 원천.

**Panel B — tanh를 선형 활성화로 바꿨을 때**

| L1 | L2 | L3 |
|---|---|---|
| 0.79 | 0.53 | 0.84 |

→ 선형 활성화로는 3층까지 깊게 가도 0.84. **비선형성 없으면 2.13은 절대 안 나온다.**

### 5-8. 강건성 검정

**대체 표본 분할 (Table A5)**
- **역순 분할**: 학습 2011.01~2020.12 / 평가 2004.07~2010.12 → EW 기준 L2 = 1.59\*\*\*, VW 기준 L2 = 1.50\*\*\*
- **랜덤 분할** (Fama-French 2018 방식, 매 분기 3개월을 3그룹에 랜덤 배정): EW 전체 L2 = 2.65, VW 전체 L2 = 2.41
- 두 경우 모두 **2층이 최고**, 시장 팩터를 크게 상회 → 원 결과와 일관.

**거래비용 반영 (Table A6)**
- 15bp / 20bp, γ ∈ {0, 0.1, 0.3, 0.5, 0.7, 0.9} 모든 조합에서 **L2가 일관되게 최고**.
- 예: 15bp, γ=0.0 → EW L2 = 1.50\*\*\*, VW L2 = 1.91\*\*\*
- 비용이 커지고 평활을 강하게 할수록 성과는 줄지만, **2층의 우위는 유지**.
- → 성과가 과도한 회전율에서 나온 게 아니라 **더 안정적이고 정보량 많은 횡단면 신호**에서 나온다는 증거.

---

## 6. 결론 (Conclusion)

### 6-1. 논문의 결론
1. **직접 비중 추정이 옳다.** μ와 Σ를 따로 추정하는 plug-in 방식의 오차 누적을 회피할 수 있고, 실제로 성능이 훨씬 좋다.
2. **벤치마크를 경제학적 prior로 명시하는 것이 핵심.** 이것이 신경망에 "경제학적 정규화(economic regularization)"를 걸어주어, 금융 데이터의 낮은 신호대잡음비 문제를 완화한다. 같은 딥러닝이라도 이 구조가 없으면(표준 ML 포트폴리오) 성능이 크게 떨어진다.
3. **비선형 롱숏 구조(소프트맥스 랭킹)가 필수.** 선형 랭킹·선형 활성화로는 재현 불가.
4. **헤지 성질은 학습의 부산물.** 샤프지수 최대화 목적이 자동으로 벤치마크와 음의 상관을 갖는 팩터를 만들어낸다. → 코로나 국면에서 실제 헤지로 작동.
5. **수익률 신호는 희소하지 않고 밀집(dense)해 있다.** 소수 특성만 쓰는 전통 팩터모형은 효율적 프론티어를 span 하지 못한다.
6. **채권·주식·옵션 정보를 모두 비선형으로 통합해야 한다.** 특히 **옵션 변수의 기여가 결정적**(2.13 → 1.01). 이는 "주식 특성이 채권 수익률과 무관하다"는 기존 일부 연구와 정면으로 배치되고, 채권-주식 시장 통합 증거와 부합한다.

### 6-2. 최종 숫자 (OOS 2014.07~2020.12, 연율화 샤프지수)
```
시장 팩터 단독          0.85 (EW) / 0.90 (VW)
딥 팩터 단독            1.00 (EW) / 1.49 (VW)
딥 접점 포트폴리오      2.13 (EW) / 2.21 (VW)   ← 최종 결과
```

### 6-3. 논문이 스스로 밝힌 한계
- OOS 평가가 **2020년 12월에 끝나서, 2021년 이후 금리 상승기는 포함되지 않음.** 지속적 금리상승 국면에서의 성과는 후속 연구 과제.
- 실증은 미국 회사채에 한정. 다만 프레임워크 자체(벤치마크 증강 논리)는 다른 자산군에 적용 가능하며, 네트워크 구조·하이퍼파라미터는 각 환경에서 다시 검증해야 함.

---

## 7. 복제(Replication) 실행 가이드

### 7-1. 전체 파이프라인

```
[1] 데이터 수집
     TRACE Enhanced + FISD  →  일중 체결 → 일별 VW 가격 → 월별 총수익률
     CRSP (rf, 주식) / Compustat (재무) / OptionMetrics (옵션)
         ↓
[2] 클리닝 & 필터 (3-2절 7단계)
         ↓
[3] 특성 132개 생성 → 월별 횡단면 rank → [-1,1] 표준화 → 결측 0
         ↓
[4] 매월 시총 상위 3,000개 선택 → (T, 3000, 132) 텐서 + (T, 3000) 수익률
         ↓
[5] 벤치마크 팩터 생성: EW 시장 / VW 시장 (필요시 TERM, DEF, SMB, HML)
         ↓
[6] 신경망 학습 (2-fold CV로 하이퍼파라미터 선택 → 인샘플 전체 재학습)
         ↓
[7] OOS 평가: 파라미터 고정 → 딥 팩터 & 딥 접점 포트폴리오 샤프지수 계산
         ↓
[8] 비교/해석: FF5·IPCA5·RP-PCA5 대비, 스패닝 회귀, gradient 중요도, 거래비용
```

### 7-2. 핵심 코드 스켈레톤 (PyTorch)

```python
import torch, torch.nn as nn

class DeepCharacteristic(nn.Module):
    """132개 특성 -> 딥 특성 스칼라 1개. 자산별로 독립 적용."""
    def __init__(self, K=132, hidden=66, n_layers=2, dropout=0.1):
        super().__init__()
        layers, d_in = [], K
        for _ in range(n_layers):
            layers += [nn.Linear(d_in, hidden), nn.Tanh(), nn.Dropout(dropout)]
            d_in = hidden
        layers += [nn.Linear(d_in, 1), nn.Tanh()]   # 최종 스칼라
        self.net = nn.Sequential(*layers)

    def forward(self, Z):        # Z: (T, N, K)
        return self.net(Z).squeeze(-1)   # -> (T, N)


def softmax_rank_weights(z, a1=50.0, a2=8.0):
    """식 (16): 미분 가능한 롱숏 정렬. z: (T, N) -> W: (T, N), 행 합 = 0"""
    x_plus  = -a1 * torch.exp(-a2 * z)
    x_minus = -a1 * torch.exp( a2 * z)
    w_long  = torch.softmax(x_plus,  dim=-1)   # 합 = 1
    w_short = torch.softmax(x_minus, dim=-1)   # 합 = 1
    return w_long - w_short                    # 롱 +1, 숏 -1, 총합 0


def sharpe2(F):
    """식 (19): SR^2 = E[F]' Cov(F)^{-1} E[F].  F: (T, P+1)"""
    mu  = F.mean(dim=0)                                  # (P+1,)
    Fc  = F - mu
    cov = (Fc.T @ Fc) / (F.shape[0] - 1)                 # (P+1, P+1)
    cov = cov + 1e-8 * torch.eye(cov.shape[0])           # 수치 안정화
    return mu @ torch.linalg.solve(cov, mu)


def loss_fn(F):
    """식 (20): exp(-SR^2). 미니배치 + 드롭아웃 하에서 계산."""
    return torch.exp(-sharpe2(F))


# ---- 학습 루프 (배치 = 10개월) ----
model = DeepCharacteristic(K=132, hidden=66, n_layers=2, dropout=0.1)
opt   = torch.optim.Adam(model.parameters(), lr=1e-3)

for epoch in range(400):
    for months in month_batches(T_train, batch_months=10):   # 10개월씩
        Z  = X[months]                     # (10, 3000, 132)
        r  = y[months]                     # (10, 3000)
        Rb = bench[months]                 # (10, P)   벤치마크 팩터 수익률

        z  = model(Z)                      # (10, 3000)  딥 특성
        W  = softmax_rank_weights(z)       # (10, 3000)  롱숏 비중
        Rd = (W * r).sum(dim=-1, keepdim=True)   # (10, 1)  딥 팩터 수익률
        F  = torch.cat([Rb, Rd], dim=-1)         # (10, P+1)

        loss = loss_fn(F)
        opt.zero_grad(); loss.backward(); opt.step()
```

**OOS 평가 시** — 학습된 모델을 `eval()`로 두고(드롭아웃 끄기) OOS 특성을 넣어 `Rd`를 만든 뒤,
**인샘플에서 계산한** `θ = Cov(F̃)⁻¹ E(F̃)` 를 그대로 적용해 `R^opt = F̃ · θ` 를 만들고 샤프지수를 계산합니다.
**θ를 OOS 데이터로 다시 구하면 안 됩니다** (룩어헤드 편향).

```python
# 인샘플에서 접점 비중 θ 확정
mu_is  = F_is.mean(0)
cov_is = torch.cov(F_is.T)
theta  = torch.linalg.solve(cov_is, mu_is)        # 고정

# OOS 적용
R_opt_oos = F_oos @ theta
SR_annual = R_opt_oos.mean() / R_opt_oos.std() * (12 ** 0.5)
```

### 7-3. 복제 시 흔히 틀리는 지점 체크리스트

- [ ] 특성은 **t−1 시점**, 수익률은 **t 시점** — 시차 정렬 확인 (룩어헤드 방지)
- [ ] 특성 표준화는 **매월 횡단면 단위** (전체 기간 통합 표준화 아님)
- [ ] 윈저라이즈는 **인샘플 수익률에만** 적용
- [ ] 배치 단위가 **자산이 아니라 "월"** — 샤프지수 계산에 시계열이 필요하기 때문
- [ ] 소프트맥스 후 **롱 합 +1, 숏 합 −1 정규화**
- [ ] 접점 비중 θ는 **인샘플에서 고정**, OOS에서 재추정 금지
- [ ] 하이퍼파라미터는 **2-fold CV의 검증 손실 평균**으로 선택, 그 뒤 인샘플 전체 재학습
- [ ] 샤프지수 연율화 = 월별 SR × √12
- [ ] 층 수(1/2/3)와 랭킹 방식(선형/소프트맥스)을 **모두 돌려서 표로 비교** — 논문의 핵심 주장이 여기 있음

### 7-4. 유료 데이터가 없을 때의 대체 전략

TRACE/FISD/OptionMetrics 없이도 **방법론은 그대로** 검증할 수 있습니다.

> **구현 완료**: 이 전략을 실행하는 수집 파이프라인이 `src/` + `run_pipeline.py` 에 있습니다.
> 사용법과 한계는 [README_데이터수집.md](README_데이터수집.md) 참조.
> `python run_pipeline.py --max-tickers 50 --n-assets 30` 으로 바로 시험 실행 가능하며,
> 결과물 `data/processed/panel.npz` 는 7-2절 코드 스켈레톤에 그대로 넣을 수 있는
> `(T, N, K)` 텐서입니다.

| 논문 요소 | 대체안 |
|---|---|
| 회사채 개별 수익률 | 미국/한국 **주식** 월별 수익률 (yfinance, FinanceDataReader, pykrx) 또는 회사채 ETF 바스켓 |
| 자산 수 N=3,000 | S&P500 / KOSPI200 등 수백~수천 종목 |
| 채권 특성 41개 | 가격 기반 특성으로 대체: 모멘텀(1/6/12/36/60개월), 변동성, 왜도, 첨도, 베타, 회전율, Amihud 비유동성, 최대일수익률 등 |
| 주식 특성 61개 | 재무제표 기반 (yfinance/FDR로 일부 취득 가능): BM, EP, 자산성장률, 수익성 등 |
| 옵션 특성 30개 | 개별종목 옵션 데이터 부재 → **VIX 등 시장 전체 변동성 지표를 시계열 특성으로** 근사하거나 생략. 단, 논문의 "옵션이 결정적" 결론은 검증 불가 |
| 벤치마크 팩터 | 시장지수 수익률(EW/VW), Fama-French 팩터(Ken French 라이브러리에서 무료 다운로드 가능) |

**권장 진행 순서**:
1. 소규모(예: 종목 200개 × 특성 20개)로 **파이프라인 전체를 먼저 돌려서 코드 정합성 확인**
2. 층 수 1/2/3 × 랭킹 선형/소프트맥스 → **논문의 정성적 결론(2층·소프트맥스가 최고)이 재현되는지** 확인
3. 특성/자산 수를 늘려가며 확장
4. 마지막에 거래비용·대체 표본분할 강건성 검정

> 절대 수치(2.13)는 회사채 + 132특성 + 해당 기간의 결과이므로, 다른 데이터에서 그대로 나오지 않는 것이 정상입니다.
> **복제의 목표는 숫자 일치가 아니라 "구조(벤치마크 prior + 비선형 롱숏 + 샤프 목적함수)가 성능을 만든다"는 정성적 결론의 재현**입니다.

---

## 8. 부록: 논문 표기 대조표

| 논문 기호 | 의미 |
|---|---|
| `r_t` | N×1 자산 초과수익률 벡터 (시점 t) |
| `z_{i,t-1}` | 자산 i의 K개 특성 벡터 (시점 t−1) |
| `w̃_{i,t-1}` | 벤치마크(시장) 포트폴리오 내 자산 i의 비중 |
| `w_d(·; Φ)` | 신경망이 만드는 롱숏 비중 함수 |
| `Z⁽ᴸ⁾_{t-1}` | 1×N 딥 특성 벡터 |
| `W_{t-1}` | 딥 팩터 포트폴리오 비중 (합 = 0) |
| `R_{d,t}` | 딥 팩터 수익률 |
| `R_{m,t}` / `R_{b,t}` | 시장 팩터 / 벤치마크 팩터 수익률 |
| `R_t^opt` | 딥 접점 포트폴리오 수익률 |
| `θ`, `θ_p`, `θ_d` | 벤치마크·딥 팩터 배분 비중 |
| `Φ`, `A⁽ˡ⁾`, `b⁽ˡ⁾` | 신경망 파라미터 (가중치, 편향) |
| `a₁, a₂` | 소프트맥스 랭킹 튜닝 파라미터 (50, 8) |
| `L` | 은닉층 수 (최적 = 2) |
| `γ` | 거래비용 분석의 지수평활 파라미터 |
| `λ` | 편도 거래비용 (15 / 20 bp) |

### 핵심 참고문헌
- Markowitz (1952) — 평균-분산 포트폴리오 이론
- Brandt, Santa-Clara, Valkanov (2009) — 파라메트릭 포트폴리오 정책 (이 논문의 직계 조상)
- Feng, He, Polson, Xu (2024, JFQA) — 소프트맥스 랭킹 기반 특성정렬 팩터 딥러닝
- Chen, Pelger, Zhu (2024, MS) — 자산가격결정 딥러닝, 드롭아웃 정규화, gradient 중요도
- Kelly, Pruitt, Su (2019) / Kelly, Palhares, Pruitt (2023) — IPCA
- Lettau, Pelger (2020) — RP-PCA
- Kozak, Nagel, Santosh (2020) / Kozak, Nagel (2024) — 희소성 비판, SDF span
- Dickerson, Mueller, Robotti (2023) — 회사채 팩터 가격결정
- Dick-Nielsen (2009, 2014) — TRACE 클리닝 표준
- Barillas, Shanken (2017) — 샤프지수 개선 유의성 검정
